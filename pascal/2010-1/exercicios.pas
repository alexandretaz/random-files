{
	1. Criar m tipo de registro com 5 campos ( a sua escolha) e uma tabela com 10 registros deste tipo (incluir os campos: nome, altura e peso do tipo real da sua escolha), e fazer as seguintes rotinas: 
		1ª) fazer um procedimento que preencha toda a tabela.
		2ª) fazer um procedimento que exiba toda a tabebela
		3ª) fazer um procedimento que preencha somente um registro da tabela (o índice  tbm deve ser passado por parametro)
		4ª) fazer um procedimento que exiba somente um registro da tabela (o índic deve ser passado por parametro)
		5ª) fazer uma função que calcule o peso médio dos registros inseridos na tabela
		6ª) fazer uma função que retorne o nome da pessoa mais alta

}


